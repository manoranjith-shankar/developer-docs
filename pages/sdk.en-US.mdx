import Link from '@components/core/Link';
import Card, { Cards } from '@components/core/Card';
import guides from './sdk/_meta.en-US.json';

# Runtime-connector SDK

`runtime-connector` is a JS library that allows you to connect to the runtime of a dapp on dataverseos.


<Cards
  num={2}
  children={Object.keys(guides).map((key, i) => (
    <Card
      key={i}
      title={guides[key].title}
      description={guides[key].description}
      icon={guides[key].icon}
      href={guides[key].href}
    />
  ))}
/>